<section id="hero">
  <%= render "hero" %>
</section>

<% if Flipper.enabled?(:enable_about) %>
  <section id="about">
    <%= render "about" %>
  </section>
<% end %>

<% if Flipper.enabled?(:enable_weekly_deals) %>
  <section id="our_restaurant">
    <%= render "our_restaurant" %>
  </section>
<% end %>

<% if Flipper.enabled?(:enable_meet_the_team) %>
  <section id="team">
    <%= render "team" %>
  </section>
<% end %>

<% if Flipper.enabled?(:enable_gallery) %>
  <section id="gallery">
    <%= render "gallery" %>
  </section>
<% end %>

<% if Flipper.enabled?(:enable_testimonials) %>
  <section id="testimonial">
    <%= render "testimonial" %>
  </section>
<% end %>

<section id="menu">
  <%= render "menu" %>
</section>

<% if Flipper.enabled?(:enable_subscribe) %>
  <section id="subscribe">
    <%= render "subscribe" %>
  </section>
<% end %>

<section id="contact">
  <%= render "contact" %>
</section>

<%= stylesheet_link_tag "active_admin" %>

<meta name="viewport" content="width=device-width,initial-scale=1">
<%= csrf_meta_tags %>
<%= csp_meta_tag %>
<%= current_admin_user_meta_tags %>

<% # On page load or when changing themes, best to add inline in `head` to avoid FOUC %>
<%= javascript_tag nonce: true do %>
  if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
    document.documentElement.classList.add('dark')
  } else {
    document.documentElement.classList.remove('dark')
  }
<% end %>
<%= javascript_importmap_tags "active_admin", importmap: ActiveAdmin.importmap %>

<%= stylesheet_link_tag "direct_uploads" %>
<%= stylesheet_link_tag "image_attachment_input" %>
<%= stylesheet_link_tag "simple_calendar" %>
<%= stylesheet_link_tag "avatar" %>
<%= javascript_include_tag "initializers/current.js" %>
<%= javascript_include_tag "direct_uploads" %>
<%= javascript_include_tag "image_attachment" %>
